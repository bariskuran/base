import { Base } from "./tools/_Base";
import { componentCreator } from "../componentCreator/index.jsx";

export const ScrollBar = componentCreator("ScrollBar", Base, {
    // error: (p) => <Base bgColor="error" {...p} />,
});
